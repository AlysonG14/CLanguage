#include <stdio.h>
#include <math.h>

int first_exercise(){

    int num1,num2;
    int op;

    float i, f;

    for(i = 0; i <= 10; x += 0.1){
      f = pow(i, 3) - 6*pow(i, 2) + 11*pow(i, 1) - 6;
      printf("Para o valor %f, a saída está dando %f", i, f)
    };



    return 0;
}